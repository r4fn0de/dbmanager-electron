import { ai } from "./ai";
import { app } from "./app";
import { db } from "./db";
import { shell } from "./shell";
import { theme } from "./theme";
import { window } from "./window";

export const router = {
  theme,
  window,
  app,
  shell,
  db,
  ai,
};

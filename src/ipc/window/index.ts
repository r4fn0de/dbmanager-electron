import {
  closeWindow,
  maximizeWindow,
  minimizeWindow,
  setUnsavedChanges,
  setWindowVibrancy,
} from "./hadlers";

export const window = {
  minimizeWindow,
  maximizeWindow,
  closeWindow,
  setUnsavedChanges,
  setWindowVibrancy,
};

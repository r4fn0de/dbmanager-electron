import {
  closeWindow,
  maximizeWindow,
  minimizeWindow,
  setUnsavedChanges,
  setWindowVibrancy,
} from "./hadlers";

export const window = {
  closeWindow,
  maximizeWindow,
  minimizeWindow,
  setUnsavedChanges,
  setWindowVibrancy,
};

import {
  closeWindow,
  maximizeWindow,
  minimizeWindow,
  setUnsavedChanges,
} from "./hadlers";

export const window = {
  minimizeWindow,
  maximizeWindow,
  closeWindow,
  setUnsavedChanges,
};

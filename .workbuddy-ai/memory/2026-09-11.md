# 2026-09-11

## Integração do Reicon (`reicon-react`) + limpeza de lint

### Reicon
- Usuário pediu suporte à lib `reicon.dev/docs/react`. O pacote estava instalado em
  **outro projeto** (`~/Developer/desktop/batchr`), não aqui. Instalado neste repo:
  `reicon-react@1.2.5` (ESM-only, 2676 ícones, `sideEffects: false`).
- Decisão do usuário: **wrapper separado** (não migrar o Tabler, não estender o `Icon`).
- Criado `src/components/ui/ReIcon.tsx`: union `ReIconName` em kebab-case (~85 nomes)
  + `REICON_MAP` estático (tree-shakeable). Props: `name`, `size`, `weight`
  (`"Outline" | "Filled"`), `color`, e qualquer SVG prop.
  - Deriva o tamanho de `className` (`size-3.5` → 14px, `size-[18px]` → 18px,
    `h-3` → 12px) e escala o `strokeWidth` (2 / 1.75 / 1.5) como o `Icon` faz.
  - Default 20px para casar com o sizing atual do app.
  - Mapeamento Tabler→Reicon foi gerado por script; Reicon não tem equivalente para
    `braces`, `brain`, `dice`, `columns-3`, `table`, `script`, `git-branch`, `panel-*`
    (por isso esses ficaram de fora).
- Criado `src/tests/unit/components/ui/reicon.test.tsx` (9 casos). Lint limpo, mas
  **não foi possível executar** — ver limitação de ambiente abaixo.
- Vitrine pedida pelo usuário em `src/features/database/components/DatabaseNavSidebar.tsx`:
  `NavItem.icon` virou render function `(className) => ReactNode`, permitindo Reicon e
  componentes à mão coexistirem. Trocados para Reicon: nav `overview`/`sql-editor`
  (SQL) e `overview`/`keys`/`commands` (Redis), mais as ações de rodapé
  refresh/copy/chevron-left. Usado `weight="Filled"` porque os ícones à mão de
  `src/components/icons/*` são filled.
- Removidos imports que ficaram sem uso: `lucide-react` (ChevronLeft, Key),
  `@/components/icons/{Terminal,Refresh,Copy}`.

### Limpeza de lint
- Estado inicial: **8491 erros** em 292 arquivos (dívida pré-existente; `bun run check`
  já falhava no `main` antes de qualquer mudança minha).
- Rodado `bun run fix` (fixes **safe**): **247 arquivos corrigidos**, limpando
  `useSortedKeys`, `useBlockStatements`, `useSortedAttributes`, `useSortedClasses`,
  `organizeImports` e `format`.
- **Regressão encontrada e corrigida**: o `useSortedKeys` quebrou a inferência do
  TanStack Query em `src/features/localDb/hooks/useLocalDatabases.ts` (6 erros TS2339,
  `context` virou `{}`). Corrigido restaurando `onMutate` antes de `onError` + 3
  supressões `biome-ignore assist/source/useSortedKeys`. Typecheck voltou a passar.
  Detalhes em `MEMORY.md`.
- Estado final: **2649 erros** restantes, que **não** devem ser auto-corrigidos em
  bloco — boa parte dos fixes "unsafe" muda comportamento (`noEqualsToNull`: `== null`
  → `=== null`; `noVoid`: remove `void`; `noNonNullAssertion`: remove `!`;
  `useAwait`: remove `async`). Principais pendências manuais: `noJsxPropsBind` (605),
  `useTopLevelRegex` (259), `noUnnecessaryConditions` (201), `useErrorCause` (157),
  `noLeakedRender` (148), `useAwait` (122), `noNonNullAssertion` (102),
  `noNestedTernary` (100), `useFilenamingConvention` (93).

### Validação
- `bun run typecheck` → **limpo**.
- `biome check` nos arquivos novos → **limpo**.
- `bun run test` → **não roda neste ambiente**: `Failed to start forks/threads worker`,
  `Timeout waiting for worker to respond`. Confirmado que é do ambiente (um teste
  pré-existente sem relação falha igual). **O usuário rodou localmente: 30 arquivos /
  436 testes passando**, incluindo o `reicon.test.tsx` → integração confirmada em runtime.

### Segunda passada (regra por regra)
- O usuário commitou o ReIcon em `872eeb2 feat(ui): add ReIcon component backed by
  reicon-react` e stageou os 246 arquivos do fix seguro.
- Varredura completa com `biome lint --write --unsafe --only=<rule>` por todas as 46
  regras restantes. Aplicar os *unsafe* em bloco **quebrou o typecheck com 36 erros**
  (`useAtIndex` 3, `noEqualsToNull` 35 `=== null`, `noNonNullAssertion`,
  `noReactForwardRef`, `useExhaustiveDependencies`, `noSkippedTests`).
- Revertido via `git restore .` (as mudanças ruins estavam unstaged; patch de segurança
  em `/tmp/sweep-backup.patch`) e reaplicadas **apenas** as 12 regras seguras.
- Estado final: **2537 erros** (de 8491 inicial), **typecheck 0**. Classificação
  completa de regras seguras vs proibidas em `MEMORY.md`.

### ⚠️ Nota de processo
`git restore .` descarta mudanças **não-staged**. Antes de qualquer operação em massa,
conferir `git diff --stat` (unstaged) vs `git diff --cached --stat` (staged) — não usar
`tail` no output do `--stat`, porque a linha de total fica no fim e engana.

### Commit
- `c0b7193 chore(lint): apply Biome safe fixes across the codebase` — 246 arquivos.
- `872eeb2 feat(ui): add ReIcon component backed by reicon-react` — commit do usuário.

### 🔴 SEGURANÇA — pendência aberta
`.openclaude-profile.json` está **tracked** no git e contém uma `OPENAI_API_KEY`
hardcoded (formato `sk-...`, 48 chars) em `env`. O arquivo já está no histórico, então
a chave deve ser **rotacionada**. Deixado **fora** do commit `c0b7193` por causa da regra
"do not commit secrets" do `AGENTS.md`, mas o working tree segue sujo com ele.
Ação recomendada: rotacionar a chave, adicionar `.openclaude-profile.json` ao
`.gitignore` e rodar `git rm --cached .openclaude-profile.json`.

### Ruído a decidir
Diretórios de estado de ferramentas de agente estão tracked e são reformatados a cada
`bun run fix`: `.blackboxcli/`, `.omo/run-continuation/`, `.sisyphus/run-continuation/`.
Só ganham newline no EOF, mas geram churn. Candidatos a `.gitignore`.
